# community-artifact-consolidation (Slice 17.12)

Authoritative local runtime artifact root: `.codestrata-artifacts/`.

## Run

```bash
.venv/bin/python -m verification.community_artifact_consolidation
```

Report:

`.codestrata-artifacts/validation/suites/sv17-12/community-artifact-consolidation-verification.json`
