# Sample Java App

Minimal Maven Java sample for CodeStrata assessments.

```bash
codestrata assess --repo examples/sample-java-app --output reports --no-ai
```

Expected: technologies include Java / Maven; deterministic `report.html` under
`reports/sample-java-app/<timestamp>/`.
