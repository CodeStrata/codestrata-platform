# Sample Python App

Minimal Python sample for CodeStrata assessments.

```bash
codestrata assess --repo examples/sample-python-app --output reports --no-ai
```

Expected: Python detected; deterministic reports under
`reports/sample-python-app/<timestamp>/`.
