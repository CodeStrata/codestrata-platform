# Ordinary handler notes (negative control)

This file describes an ordinary application handler. It must **not** become
serverless evidence.

Words present for negative-control review only: function, handler, region,
bucket, queue. No serverless framework config (`serverless.yml`, SAM, Azure
Functions host.json) exists in this fixture.
