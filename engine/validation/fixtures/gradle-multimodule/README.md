# gradle-multimodule (controlled fixture)

Minimal Gradle multi-module Java project for Epic 4 Slice 4.13.

## Structure

- `settings.gradle` includes `:app` and `:lib`
- `app/` and `lib/` each have `build.gradle` and one Java source file

## Safety

- Static sources only; no credentials
- CodeStrata-owned validation fixture
