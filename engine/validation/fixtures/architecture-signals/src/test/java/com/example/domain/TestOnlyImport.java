package com.example.domain;

import com.example.persistence.Repo;

/**
 * TEST-ONLY: test source importing persistence.
 * Must not become a production architecture dependency edge.
 */
public class TestOnlyImport {
    private Repo repo;
}
