[project]
name = "demo"
version = "0.1.0"
dependencies = [
  "requests>=2.31.0",
  "PyYAML==6.0.1",
]
[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.poetry.dependencies]
python = "^3.12"
httpx = "0.27.0"

[tool.poetry.group.dev.dependencies]
ruff = "0.5.0"
