"""Thin coverage wrapper for priority."""

from __future__ import annotations

def test_priority_importable() -> None:
    from verification.assessment_consistency import priority
    assert priority
