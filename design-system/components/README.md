# Components

Machine catalog: [`catalog.json`](catalog.json)

All components are **contracts** for later slices. Slice 14.1 does not ship
rendered component libraries into docs, reports, VS Code, or Marketplace.

Required IDs: button, card, table, panel, sidebar, navigation, tabs, progress,
chart, badge, alert, score_card, evidence_card, recommendation_card.
