# Design tokens (Swagger / Platform API docs)

Synced from `docs/public/design-tokens/tokens.css`.

Authority: `codestrata-visual-design-system:1.0` (not amber-era website capture).

```bash
cp docs/public/design-tokens/tokens.css \
   platform/api/openapi/swagger/design-tokens/tokens.css
```
