PortfolioModernizationSummaryDto:
  properties:
    candidates:
      items:
        $ref: '#/components/schemas/ModernizationCandidateDto'
      type: array
      title: Candidates
    theme_counts:
      items:
        prefixItems:
        - type: string
        - type: integer
        type: array
        maxItems: 2
        minItems: 2
      type: array
      title: Theme Counts
    wave_counts:
      items:
        prefixItems:
        - type: string
        - type: integer
        type: array
        maxItems: 2
        minItems: 2
      type: array
      title: Wave Counts
    policy_version:
      type: string
      title: Policy Version
  additionalProperties: false
  type: object
  required:
  - policy_version
  title: PortfolioModernizationSummaryDto
